# Optional for a persistent local JWT key. Generate a random secret; never commit it.
JWT_SECRET=
# Without Docker, SQLite is the zero-setup local demo fallback.
DATABASE_URL=sqlite:///./nexus.db
# Docker Compose requires you to set this locally before starting.
POSTGRES_PASSWORD=
